% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation_Analysis.R
\name{sequencing}
\alias{sequencing}
\title{Simulate sequencing from tumor data}
\usage{
sequencing(
  Zprovv = NULL,
  Clones_df = NULL,
  parameters,
  seed = NULL,
  n_regions = 1,
  n_seq_cells = 1000,
  Nrep = 1,
  dens = NULL
)
}
\arguments{
\item{Zprovv}{A list representing a sequenced tumor slice. Required if
\code{Clones_df} is not provided.}

\item{Clones_df}{A tibble of ordered clones as returned by
\code{get_ordered_clones_sequencing()}. If provided, \code{Zprovv} is not required.}

\item{parameters}{An object of class \code{Parameters} containing model parameters.}

\item{seed}{Optional numeric seed for reproducibility. If \code{NULL}, a seed is generated.}

\item{n_regions}{Integer. Number of spatial regions to sequence (1 = bulk).}

\item{n_seq_cells}{Integer. Number of cells sequenced per region. Use \code{Inf}
to sequence the entire tumor mass.}

\item{Nrep}{Integer. Number of independent sequencing repetitions.}

\item{dens}{An object with class "density" containing the density used to sample sequencing depth (DP).
If NULL, a default density derived from TCGA data is used.}
}
\value{
A list of tibbles (length \code{Nrep}), each representing a simulated
sequencing experiment. Each tibble contains:
\itemize{
\item \code{region}: region identifier
\item \code{mut}: mutation identifier
\item \code{fun_eff}: functional effect label
\item \code{fun_eff_category}: functional effect category
\item \code{sample_DP}: sampled read depth
\item \code{sample_AD}: sampled alternative allele count
\item \code{VAF}: variant allele frequency
}
}
\description{
This function simulates bulk or multi-regional sequencing starting from either
a tumor slice (\code{Zprovv}) or a precomputed ordered clone table (\code{Clones_df}).
It generates one or more VCF-like tibbles containing sampled variant allele
frequencies (VAF), read depths (DP), and alternative allele counts (AD).
}
\details{
If \code{Clones_df} is not provided, it is computed internally from \code{Zprovv}.
The function assumes that clone genotypes and functional effects are consistent
with the provided \code{parameters}.
}
\examples{
\dontrun{
res <- sequencing(
  Zprovv = Zprovv,
  parameters = parameters,
  n_regions = 2,
  n_seq_cells = 500,
  Nrep = 3
)
}

}
